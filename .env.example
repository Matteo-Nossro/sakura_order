# Variables d'environnement pour le stack Sakura Order
# Ces variables se configurent dans l'onglet "Environment variables" du stack Portainer,
# pas dans un fichier .env versionné.

# === Configuration ===
NODE_ENV=production
HOST=0.0.0.0
PORT=3000
